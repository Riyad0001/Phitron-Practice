#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> f = {99, 7, 9, 5};
    // cout << f.front();
    // cout << f.back();
    for (auto it = f.begin(); it < f.end(); it++)
        cout << *it << " ";

    return 0;
}