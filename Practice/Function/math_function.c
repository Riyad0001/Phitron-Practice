#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    // ceil function

    // double x;
    // scanf("%lf", &x);
    // int ans = ceil(x);
    // printf("%d", ans);

    // floor function

    // double x;
    // scanf("%lf", &x);
    // int ans = floor(x);
    // printf("%d", ans);

    // round function

    // double x;
    // scanf("%lf", &x);
    // int ans = round(x);
    // printf("%d", ans);

    // sqrt function

    // double x;
    // scanf("%lf", &x);
    // double ans = sqrt(x);
    // printf("%0.2lf", ans);

    // pow function

    // double x, y;
    // scanf("%lf %lf", &x, &y);
    // double ans = pow(x, y);
    // printf("%0.2lf", ans);

    // abs function

    int x;
    scanf("%d", &x);
    int p = abs(x);
    printf("%d", p);

    return 0;
}