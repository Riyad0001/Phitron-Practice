#include <stdio.h>
int main()
{

    int n, s, y;
    scanf("%d", &n);
    s = 0;
    y = 2 * n - 1;
    for (int i = 0; i < n; i++)
    {

        for (int j = 0; j < count; j++)
        {
            /* code */
        }
    }

    return 0;
}